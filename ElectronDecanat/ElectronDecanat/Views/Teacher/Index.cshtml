@using ElectronDecanat.Code;
@model List<TeacherWork>
@{
    ViewBag.Title = "Преподаватель";
}
<body>
    <table id="main-table">
        <thead>
            <tr>
                <th>Преподаватель</th>
                <th>Специальность</th>
                <th>Дисциплина</th>
                <th>Курс-группа-подгруппа</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @{
                for (int i = 0; i < Model.Count; i++)
                {

                    <tr>
                        <td>@Model[i].teacher_name</td>
                        <td>@Model[i].speciality_name (@Model[i].speciality_code)</td>
                        <td>@Model[i].discipline_name</td>
                        <td>@Model[i].coors-@Model[i].group_number-@Model[i].subgroop_number</td>
                        <td class="clicable" onclick="this.firstChild.click()">@Html.ActionLink("Подробнее", "Labs", new { discipline = Model[i].discipline_name })</td>
                    </tr>
                }
            }
        </tbody>
    </table>
    <script type="text/javascript">
        document.onload = new function () 
        {
            $('#main-table').DataTable(russian_lang);
        }
    </script>
</body>
